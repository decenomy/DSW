bin_PROGRAMS += bench/bench___decenomy__
BENCH_SRCDIR = bench
BENCH_BINARY = bench/bench___decenomy__$(EXEEXT)


bench_bench___decenomy___SOURCES = \
  bench/bench_pivx.cpp \
  bench/bench.cpp \
  bench/bench.h \
  bench/Examples.cpp \
  bench/base58.cpp \
  bench/checkqueue.cpp \
  bench/crypto_hash.cpp \
  bench/perf.cpp \
  bench/perf.h \
  bench/prevector_destructor.cpp

bench_bench___decenomy___CPPFLAGS = $(AM_CPPFLAGS) $(BITCOIN_INCLUDES) $(EVENT_CFLAGS) $(EVENT_PTHREADS_CFLAGS) -I$(builddir)/bench/
bench_bench___decenomy___CXXFLAGS = $(AM_CXXFLAGS) $(PIE_FLAGS)
bench_bench___decenomy___LDADD = \
  $(LIBBITCOIN_SERVER) \
  $(LIBBITCOIN_COMMON) \
  $(LIBBITCOIN_UNIVALUE) \
  $(LIBBITCOIN_UTIL) \
  $(LIBBITCOIN_CRYPTO) \
  $(LIBLEVELDB) \
  $(LIBMEMENV) \
  $(LIBSECP256K1) \
  $(LIBBITCOIN_ZEROCOIN) \
  $(LIBRUSTZCASH) \
  $(LIBZCASH_LIBS)

if ENABLE_ZMQ
bench_bench___decenomy___LDADD += $(LIBBITCOIN_ZMQ) $(ZMQ_LIBS)
endif

if ENABLE_WALLET
bench_bench___decenomy___LDADD += $(LIBBITCOIN_WALLET)
endif

bench_bench___decenomy___LDADD += $(LIBBITCOIN_CONSENSUS) $(BOOST_LIBS) $(BDB_LIBS) $(SSL_LIBS) $(CRYPTO_LIBS) $(MINIUPNPC_LIBS) $(EVENT_PTHREADS_LIBS) $(EVENT_LIBS)
bench_bench___decenomy___LDFLAGS = $(RELDFLAGS) $(AM_LDFLAGS) $(LIBTOOL_APP_LDFLAGS)


CLEAN_BITCOIN_BENCH = bench/*.gcda bench/*.gcno

CLEANFILES += $(CLEAN_BITCOIN_BENCH)

bitcoin_bench: $(BENCH_BINARY)

bench: $(BENCH_BINARY) FORCE
	$(BENCH_BINARY)

bitcoin_bench_clean : FORCE
	rm -f $(CLEAN_BITCOIN_BENCH) $(bench_bench___decenomy___OBJECTS) $(BENCH_BINARY)